#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ull;
typedef long long ll;

int n;
ll x;
vector<ll> subsets;

void search(vector<ll> arr){
	
}

int main(){
	cin >> n >> x;
	vector<ll> arr1(n/2);
	vector<ll> arr2(n-n/2);
	for(int i=0; i<n/2; i++){
		cin >> arr1[i];
	}
	for(int i=0; i<n-n/2; i++){
		cin >> arr2[i];
	}
	
	
}
